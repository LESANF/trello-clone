import styled from 'styled-components';

const Wrapper = styled.div`
    height: 100px;
    background-color: blue;
    margin-top: 200px;
    margin-bottom: 50px;
`;

function BoardForm() {
    return <Wrapper>BoardForm</Wrapper>;
}

export default BoardForm;
